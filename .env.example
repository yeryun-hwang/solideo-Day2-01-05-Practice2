# No API keys required! Using OpenStreetMap (free, worldwide)
# OpenStreetMap services used:
# - Nominatim (geocoding)
# - OSRM (routing)
# - Overpass API (place search)

# Server Configuration
PORT=5000
NODE_ENV=development
